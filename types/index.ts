export type MenuItem = {
  id: string;
  name: string;
  description: string;
  price: string;
  imageAlt: string;
  imageSrc: string;
};

export type Notice = {
  title: string;
  description: string;
};
